# BSD-000-03: 共通 UI設計

## 概要

@harborix/ui（共通UIライブラリ）のコンポーネント設計を定義する。

## デザイン方針

| 項目 | 方針 |
| ---- | ---- |
| スタイリング | Tailwind CSS |
| アイコン | Lucide React |
| アニメーション | Tailwind + CSS transitions |
| アクセシビリティ | WAI-ARIA準拠 |

## デザイントークン

### カラーパレット

#### プライマリ・セカンダリ

| トークン | ライトモード | ダークモード | 用途 |
| -------- | ------------ | ------------ | ---- |
| primary | #2563EB (blue-600) | #3B82F6 (blue-500) | メインアクション |
| primary-hover | #1D4ED8 (blue-700) | #60A5FA (blue-400) | ホバー時 |
| secondary | #64748B (slate-500) | #94A3B8 (slate-400) | サブアクション |
| secondary-hover | #475569 (slate-600) | #CBD5E1 (slate-300) | ホバー時 |

#### 背景・サーフェス

| トークン | ライトモード | ダークモード | 用途 |
| -------- | ------------ | ------------ | ---- |
| background | #FFFFFF | #0F172A (slate-900) | ページ背景 |
| surface | #F8FAFC (slate-50) | #1E293B (slate-800) | カード背景 |
| surface-hover | #F1F5F9 (slate-100) | #334155 (slate-700) | ホバー時 |
| border | #E2E8F0 (slate-200) | #334155 (slate-700) | 境界線 |

#### テキスト

| トークン | ライトモード | ダークモード | 用途 |
| -------- | ------------ | ------------ | ---- |
| text-primary | #0F172A (slate-900) | #F8FAFC (slate-50) | 本文 |
| text-secondary | #64748B (slate-500) | #94A3B8 (slate-400) | 補足テキスト |
| text-muted | #94A3B8 (slate-400) | #64748B (slate-500) | 薄いテキスト |
| text-inverse | #FFFFFF | #0F172A (slate-900) | 反転テキスト |

#### ステータス

| トークン | ライトモード | ダークモード | 用途 |
| -------- | ------------ | ------------ | ---- |
| success | #16A34A (green-600) | #22C55E (green-500) | 成功 |
| warning | #D97706 (amber-600) | #F59E0B (amber-500) | 警告 |
| error | #DC2626 (red-600) | #EF4444 (red-500) | エラー |
| info | #0284C7 (sky-600) | #0EA5E9 (sky-500) | 情報 |

### タイポグラフィ

#### フォントファミリー

| 用途 | フォント |
| ---- | -------- |
| 見出し・本文 | Inter, system-ui, sans-serif |
| 等幅 | JetBrains Mono, monospace |

#### フォントサイズ

| トークン | サイズ | 行高 | ウェイト | 用途 |
| -------- | ------ | ---- | -------- | ---- |
| heading-1 | 30px | 1.2 | 700 (bold) | ページタイトル |
| heading-2 | 24px | 1.3 | 600 (semibold) | セクション見出し |
| heading-3 | 20px | 1.4 | 600 (semibold) | サブセクション |
| heading-4 | 16px | 1.4 | 600 (semibold) | 小見出し |
| body | 14px | 1.5 | 400 (normal) | 本文 |
| body-lg | 16px | 1.5 | 400 (normal) | 大きめ本文 |
| caption | 12px | 1.4 | 400 (normal) | 補足テキスト |
| label | 14px | 1.4 | 500 (medium) | ラベル |

### スペーシング

4pxベースのスケールを使用。

| トークン | 値 | 用途 |
| -------- | -- | ---- |
| 0 | 0px | なし |
| 1 | 4px | 極小 |
| 2 | 8px | 小 |
| 3 | 12px | やや小 |
| 4 | 16px | 標準 |
| 5 | 20px | やや大 |
| 6 | 24px | 大 |
| 8 | 32px | 特大 |
| 10 | 40px | セクション間 |
| 12 | 48px | ページ余白 |

### 角丸

| トークン | 値 | 用途 |
| -------- | -- | ---- |
| none | 0px | 角丸なし |
| sm | 4px | 小さい要素 |
| md | 8px | ボタン、入力 |
| lg | 12px | カード |
| xl | 16px | モーダル |
| full | 9999px | 丸形 |

### シャドウ

| トークン | 値 | 用途 |
| -------- | -- | ---- |
| sm | 0 1px 2px rgba(0,0,0,0.05) | 軽い浮き |
| md | 0 4px 6px rgba(0,0,0,0.1) | カード |
| lg | 0 10px 15px rgba(0,0,0,0.1) | ドロップダウン |
| xl | 0 20px 25px rgba(0,0,0,0.15) | モーダル |

## コンポーネント一覧

### 基本コンポーネント

| コンポーネント | 概要 | バリアント |
| -------------- | ---- | ---------- |
| Button | ボタン | primary, secondary, outline, ghost, destructive |
| Input | テキスト入力 | default, error, disabled |
| Select | セレクトボックス | default, error |
| Checkbox | チェックボックス | default, disabled |
| Radio | ラジオボタン | default, disabled |
| Switch | トグルスイッチ | default, disabled |
| Textarea | 複数行入力 | default, error |

### レイアウト

| コンポーネント | 概要 |
| -------------- | ---- |
| Container | コンテンツ幅制限（max-width: 1280px） |
| Stack | 縦方向並び（gap指定可能） |
| HStack | 横方向並び（gap指定可能） |
| Grid | グリッドレイアウト |
| Sidebar | サイドバーレイアウト（幅: 240px） |

### ナビゲーション

| コンポーネント | 概要 |
| -------------- | ---- |
| Header | ヘッダー（高さ: 64px） |
| BottomNav | ボトムナビゲーション（高さ: 56px） |
| Breadcrumb | パンくずリスト |
| Tabs | タブ |
| NavLink | ナビゲーションリンク |

### フィードバック

| コンポーネント | 概要 |
| -------------- | ---- |
| Toast | 通知メッセージ（自動消去: 5秒） |
| Alert | アラート（success, warning, error, info） |
| Dialog | 確認ダイアログ |
| Modal | モーダル |
| Loading | ローディングスピナー |
| Skeleton | スケルトンローダー |

### データ表示

| コンポーネント | 概要 |
| -------------- | ---- |
| Card | カード |
| Table | テーブル |
| List | リスト |
| Badge | バッジ（ステータス表示） |
| Avatar | アバター（サイズ: sm/md/lg） |
| EmptyState | 空状態の表示 |

### フォーム

| コンポーネント | 概要 |
| -------------- | ---- |
| Form | フォームコンテナ |
| FormField | フォームフィールド（ラベル + 入力 + エラー） |
| DatePicker | 日付選択 |
| TimePicker | 時間選択 |
| FileUpload | ファイルアップロード |

## テーマ切り替え

### ThemeProvider

```tsx
import { createContext, useContext, useEffect, useState } from 'react'

type Theme = 'light' | 'dark' | 'system'

interface ThemeContextType {
  theme: Theme
  setTheme: (theme: Theme) => void
  resolvedTheme: 'light' | 'dark'
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined)

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>('system')
  const [resolvedTheme, setResolvedTheme] = useState<'light' | 'dark'>('light')

  useEffect(() => {
    const root = document.documentElement
    const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark'
      : 'light'

    const resolved = theme === 'system' ? systemTheme : theme
    setResolvedTheme(resolved)

    root.classList.remove('light', 'dark')
    root.classList.add(resolved)
  }, [theme])

  return (
    <ThemeContext.Provider value={{ theme, setTheme, resolvedTheme }}>
      {children}
    </ThemeContext.Provider>
  )
}
```

### useTheme Hook

```tsx
export function useTheme() {
  const context = useContext(ThemeContext)
  if (!context) {
    throw new Error('useTheme must be used within a ThemeProvider')
  }
  return context
}

// 使用例
function ThemeToggle() {
  const { theme, setTheme, resolvedTheme } = useTheme()

  return (
    <button onClick={() => setTheme(resolvedTheme === 'light' ? 'dark' : 'light')}>
      {resolvedTheme === 'light' ? '🌙' : '☀️'}
    </button>
  )
}
```

### Tailwind設定

```javascript
// tailwind.config.js
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: 'var(--color-primary)',
          hover: 'var(--color-primary-hover)',
        },
        // ... 他のカラー定義
      },
    },
  },
}
```

## 変更履歴

- 2026-02-12: デザイントークン詳細を確定
- 2026-02-12: TODO項目を整理
- 2026-02-11: 初版作成（テンプレート）
